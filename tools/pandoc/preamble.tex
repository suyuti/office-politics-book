\usepackage{graphicx}
\usepackage{float}
\usepackage{hyperref}
\usepackage{setspace}
\usepackage{titlesec}
\usepackage{fancyhdr}

% Statik font dosyalarını açıkça tanımla: değişken font yerine belirli ağırlıkları kullan
\setmainfont{Montserrat}[
  UprightFont   = Montserrat-Regular,
  BoldFont      = Montserrat-Bold,
  ItalicFont    = Montserrat-Italic,
  BoldItalicFont = Montserrat-BoldItalic
]

% Varsayılan görsel boyutu: sayfa genişliğini aşmasın
\setkeys{Gin}{width=\linewidth,keepaspectratio}

% Görsellerin tam olarak yerleştirileceği yeri zorlamak için
\floatplacement{figure}{H}

\setstretch{1.15}

\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}

\titleformat{\chapter}[display]
  {\bfseries\Huge}
  {\chaptername\ \thechapter}
  {1ex}
  {}
